# DayLib-API - API для всего Backend-а
Daylib - Это библиотека где будут хранится все возможные типы развлечений, от книг, видео и постов до манги, музыки и могого прочего контента

## Какие есть плюсы данного проекта?
- Проект имеет свой собственный фреймворк.
- Celery автоматически переносит задасу в Celery воркер при забивании стека
- Гибкость - разработчику сможет проще простого создать своего паука
- асинхронность на каждом шагу поэтому сможет работать на слабых серверах.

# Быстрый старт
```bash
cp env.example .env
nano .env # Редактируем чуствительные параметры как SECRET_KEY, SERVICE_KEY, ADMIN_LOGIN и т д.
docker-compose up -d --build
```

## Какие могут быть трудности?
```bash
bash: ./entrypoint.sh: No such file or directory
```
Что-бы его исправить пожалуйста поменяйте CRLF на LF. Если проблема повторяется попробуйте поменять `#!/bin/sh` на `#!/bin/bash`


# Сервисный ключ что это?
Вы могли заметить что в .env есть сервисный ключ, этот ключ нужен что-бы ваши сервисы могли безперебойно `долбить` по API, без JWT, или проверки на подлинность. Поэтому рекомендуемая длина не менее 32 символов, что-бы перебор был бесполезен.

## Как создать сервисный ключ?
```bash
openssl rand -hex 32
```
После создание поменяйте в `.env` поле `SERVICE_KEY`

## Как указать сервсиный ключ при запросе к API
Что-бы указать ваш ключ при запросе просто подставьте в headers `Service-Token-WWW`
```python
import requests

headers = {
    "accept": "application/json",
    "Service-Token-WWW": "YOUR-SERVICE-KEY",
}

response = requests.get("http://127.0.0.1:8000/api/v1/spider/status", headers=headers)
```

# Кто нибудь помогите?
Этому проекты нужны люди которые могут наполнить [пауков](core/spider), своими любимыми сайтами и поможет в написании тестов, для менеджеров и прочего. Так-же не помешало-бы написать некий справочник для написание тестов, как вы поняли я в этом бездарный, спасибо что дочитали до конца!